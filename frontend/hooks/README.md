Reserved for React/Next.js hooks if the dashboard is migrated to a framework. The current dashboard is intentionally
build-free (vanilla ES modules) so it can be served by FastAPI or any free static host with zero tooling.
